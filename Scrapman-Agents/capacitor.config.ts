import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scrapbot.agent',
  appName: 'Scrapbot Agent',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
